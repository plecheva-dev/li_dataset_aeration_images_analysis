This folder contain "custom_tools", a package containing functions and objects specifically designed for this data science package. Those tools includes data loading, plots, models, etc., and should be imported into scripts and notebooks. 

The tools are available everywhere in the package and should be cleanly and efficiently coded!

For code development and exploratory data analysis, use sandbox.py, but transfer finished codes to the package as soon as they are finalized.